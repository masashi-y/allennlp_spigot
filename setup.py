from setuptools import setup, find_packages

setup(
    name='spigot',
    version='0.0.0',
    # install_requires=['spigot'],
    packages=find_packages(),
    license='MIT License',
    extras_require={},
)
